import React from 'react';
import StationTwinView from './StationTwinView.jsx';

// High-fidelity image-based Digital Twin representation
export function StationCanvas(props) {
  return <StationTwinView {...props} />;
}

export default StationCanvas;
